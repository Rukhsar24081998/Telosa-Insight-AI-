# Telosa Insight environment

# When false (default), AIService uses deterministic mock analysis.
# When true, AIService uses Gemini and falls back to mock on failure.
USE_REAL_AI=false

# Required only when USE_REAL_AI=true
GEMINI_API_KEY=

# Optional model override
GEMINI_MODEL=gemini-3.6-flash
